{
  "name": "tic-tac-toe-deployment",
  "version": "1.0.0",
  "description": "Deployment package for Tic Tac Toe server",
  "scripts": {
    "start": "cd server && npm install && npm start",
    "install": "cd server && npm install"
  },
  "engines": {
    "node": "18.x"
  }
} 